# 3DLibrary

Blender で制作した 3D アセットをローカル PC 上で一元管理する Web アプリケーション。source ディレクトリを唯一の Source of Truth とし、DB とキャッシュはそこから再構築可能とする。

## Language

**アセット (Asset)**:
source 配下のカテゴリディレクトリ内にある、1 つの 3D 制作物を表すディレクトリ一式(model.blend、textures、meta.json 等)。
_Avoid_: モデル、ファイル

**ソース (Source)**:
唯一の正しいデータ。.blend ファイルとアセットメタを含む source ディレクトリ配下の全ファイル。これのみでライブラリ全体を復元できる。

**タイトル (Title)**:
アセットディレクトリの名前そのもの。タイトル変更とはディレクトリのリネームである。

**カテゴリ (Category)**:
source 直下のディレクトリ名。固定の集合ではなく、ユーザーが自由に増やす(スキャンが発見したものがすべて)。アセットは必ずいずれか 1 つのカテゴリディレクトリ直下に属する。

**アセットメタ (Asset Meta)**:
アセットディレクトリ内の meta.json に保存されるユーザー編集データ。現状はタグのみ。ソースの一部であり、復元対象。タイトル・カテゴリはここには持たない(ディレクトリ構造が正)。
_Avoid_: メタデータ(→ 抽出メタデータと衝突するため)

**タグ (Tag)**:
アセットメタに保存される自由記述の分類ラベル。いずれかのアセットに付いている間だけライブラリに存在し、最後のアセットから外れた時点で消える。未使用タグを記憶する台帳は持たない。大文字小文字は区別される(Wood と wood は別タグ)。
_Avoid_: タグ台帳、タグマスタ(→ 存在しない概念のため)

**キャッシュ (Cache)**:
ソースから再生成可能な派生データ(GLB、サムネイル、抽出メタデータ、スプライト)。いつ削除してもよい。

**スプライト (Sprite)**:
1 つのアセットを全周にわたり一定角度刻みでレンダリングした一連の静止画を、1 枚に敷き詰めた画像。一覧画面でモデルを読み込まずに全周の外観を確認するために使う。キャッシュの一部であり、ソースから再生成可能。
_Avoid_: プレビュー、プレビュー画像(→ 詳細画面の GLB ビューワによる確認と衝突するため)

**GLB ビューワ (GLB Viewer)**:
詳細画面で GLB をその場で回転・パン・ズームして確認する 3D 表示領域。ユーザー向け表記は「3D プレビュー」。
_Avoid_: プレビュー(単独表記はスプライトの禁止表記と同根で、対象が曖昧になるため)

**孤児キャッシュ (Orphan Cache)**:
source に対応するカテゴリ・アセットがもう存在しないキャッシュ。スキャンのたびに掃除(削除)される。不完全アセットのキャッシュは孤児ではない(ディレクトリが残る限りアセットは生きている)。
_Avoid_: 迷子ファイル(→ cache 配下にあるキャッシュ以外のファイルを指す別概念のため。こちらは掃除対象外)

**スキャン (Scan)**:
source ディレクトリを歩いてインデックスをファイルシステムと一致させる軽量処理。起動時と手動操作で走る。Blender は起動せず、source に対して完全に読み取り専用。併せて孤児キャッシュを掃除する(書き込むのは cache のみ)。
_Avoid_: 同期(→ 双方向の突き合わせを連想させ、source に対して読み取り専用であることを曖昧にするため)

**不完全アセット (Incomplete Asset)**:
カテゴリ直下にあるが model.blend を持たないディレクトリ。一覧には警告付きで表示されるが、プレビュー・生成・Blender 起動はできない。
_Avoid_: 無効アセット、壊れたアセット

**生成 (Generation)**:
1 回の Blender 起動で、あるアセットの GLB・サムネイル・抽出メタデータ・スプライトの 4 点を必ずまとめて作り出す重い処理。スキャンには含まれず、ユーザーの明示的な指示でのみ走る。
_Avoid_: 変換、エクスポート(→ exports への書き出しと区別するため)

**抽出メタデータ (Extracted Metadata)**:
生成時に Blender が .blend から読み取る統計情報(Object 数、Polygon 数、Animation 有無等)。キャッシュの一部であり、表示専用。
_Avoid_: メタデータ(単独表記は アセットメタ と紛れるため)

**インデックス (Index)**:
検索・高速表示のためにソースの内容を写し取った SQLite DB。キャッシュと同様、いつでも再構築可能。ユーザーデータの保存先ではない。
_Avoid_: データベース(保存庫のニュアンスを避ける)

**リモート閲覧 (Remote Viewing)**:
ネットワーク越しに届く読み取り専用のアクセス。ライブラリのすべてを見られるが、ライブラリを変える操作は存在しない。本人確認はアプリの外(ネットワーク層)で済んでいる前提とし、アプリ自身は認証を持たない。
_Avoid_: デプロイ、公開、共有、閲覧モード(→ 第 2 の実行環境・不特定への公開・状態切替を連想させるため)
