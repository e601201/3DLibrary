# 3DLibrary 操作マニュアル

3DLibrary は、Blender で作った 3D アセット(モデル)をパソコンの中で一覧・検索・プレビューできるアプリです。このマニュアルは、はじめて使う方向けに操作方法を説明します。

---

## 目次

1. [まず知っておきたい言葉](#1-まず知っておきたい言葉)
2. [アプリを起動する](#2-アプリを起動する)
3. [はじめての設定](#3-はじめての設定)
4. [アセット一覧画面の見かた](#4-アセット一覧画面の見かた)
5. [探す・絞り込む](#5-探す絞り込む)
6. [新しいアセットを作る](#6-新しいアセットを作る)
7. [サムネイルとプレビューを生成する](#7-サムネイルとプレビューを生成する)
8. [詳細画面でできること](#8-詳細画面でできること)
9. [タグを付けて整理する](#9-タグを付けて整理する)
10. [ファイルを直接追加・削除したとき(再スキャン)](#10-ファイルを直接追加削除したとき再スキャン)
11. [「要更新」と表示されたら](#11-要更新と表示されたら)
12. [キャッシュの容量確認と削除](#12-キャッシュの容量確認と削除)
13. [困ったときは](#13-困ったときは)

---

## 1. まず知っておきたい言葉

| 言葉 | 意味 |
|------|------|
| **アセット** | 1 つの 3D 作品のこと。中身は Blender のファイル(model.blend)やテクスチャ画像などをまとめたフォルダです |
| **カテゴリ** | アセットを分類するフォルダ(例: Props、Characters)。自由に増やせます |
| **ライブラリ** | すべてのアセットを保存する大元のフォルダ。最初の設定で場所を決めます |
| **タグ** | アセットに付ける目印(例: wood、furniture)。検索や絞り込みに使えます |
| **生成** | Blender を使って「サムネイル画像」「3D プレビュー用データ」「ポリゴン数などの情報」「スクラブ用の全周画像」の 4 点を作る処理。少し時間がかかります |
| **キャッシュ** | 生成で作られたデータの置き場所。**いつ削除しても元データは壊れません**(また生成すれば戻ります) |

> **大事なこと**: あなたの作品データ(.blend ファイルやタグ)はすべてライブラリ内の `source` フォルダに入っています。アプリはここを勝手に書き換えません(書き込むのは「新規作成」と「タグ編集」のときだけです)。

---

## 2. アプリを起動する

1. `3dlibrary`(Windows では `3dlibrary.exe`、macOS では `3DLibrary.command`)をダブルクリックします。
   - macOS で「開発元を検証できないため開けません」と出た場合は、[13. 困ったときは](#13-困ったときは) を見てください。
2. 自動的にブラウザが開き、アプリの画面が表示されます。
   - 開かない場合は、ブラウザで `http://127.0.0.1:8765` を開いてください。
3. アプリはこのパソコンの中だけで動きます。インターネット接続は不要で、外部にデータが送られることもありません。

終了するときは、起動時に開いた黒い画面(ターミナル)を閉じてください。

---

## 3. はじめての設定

初回起動時は「ライブラリディレクトリが設定されていません」と表示されます。**「設定を開く」** ボタン、または左サイドバー最下段の **「設定」** から設定画面を開いてください。

| 設定項目 | 説明 |
|----------|------|
| **Blender実行ファイル** | Blender 本体の場所。生成と「Blenderで開く」に使います。<br>Windows の例: `C:\Program Files\Blender Foundation\Blender 5.2\blender.exe`<br>macOS の例: `/Applications/Blender.app/Contents/MacOS/Blender` |
| **ライブラリディレクトリ** | アセットを保存するフォルダ。**先に空のフォルダを作ってから**、その場所を入力してください。自動で中に必要なフォルダ一式が作られます |
| **サムネイルサイズ** | 一覧に表示するサムネイル画像の大きさ(256px / 512px / 1024px)。あとから変更もできます |
| **テーマ** | ダーク / ライト / システム(OS の設定に合わせる)。選ぶとすぐ画面に反映されます |

入力したら右下の **「✓ 変更を保存」** を押してください。「保存しました」と出れば完了です。保存前に入力を取り消したいときは **「元に戻す」** を押します。

> **ヒント**: Blender は 5.2 LTS 以上を使ってください。

---

## 4. アセット一覧画面の見かた

```
┌────────────────┬──────────────────────────────────────────────┐
│ 3D LIBRARY     │ [🔍 検索] [カテゴリ▼] [並び順▼]  [⊞|≡] [＋新規アセット] │
│                ├──────────────────────────────────────────────┤
│ すべてのアセット 12 │ すべてのアセット  12 ASSETS          ~/3DLibrary/source │
│                │ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐        │
│ カテゴリ         │ │  🖼  │ │  🖼  │ │  🖼  │ │  ◻  │ ← アセットカード │
│  Props      8  │ └──────┘ └──────┘ └──────┘ └──────┘        │
│  Characters 4  │                                              │
│                │                                              │
│ タグ            │                                              │
│ [wood] [pbr]   │                                              │
│                │                                              │
│ ┌────────────┐ │                                              │
│ │キャッシュ 1.2MB│ │                                              │
│ │[↻ 再スキャン] │ │                                              │
│ │[⚡ 一括生成(3)]│ │                                              │
│ └────────────┘ │                                              │
│ ⚙ 設定          │                                              │
└────────────────┴──────────────────────────────────────────────┘
```

- 画面は**左のサイドバー**と**右の作業エリア**に分かれています。カテゴリ・タグ・キャッシュ・設定はすべて左サイドバーにあります。
- **カード**にはサムネイル・名前・カテゴリ・ポリゴン数・更新日・ファイルサイズが表示されます。
- サムネイルがまだ無いアセットは **「サムネイル未生成」** と表示され、その場に **「⚡ 生成」** ボタンが出ます。→ [7. 生成する](#7-サムネイルとプレビューを生成する)
- ポリゴン数が「—」なのも同じ理由です(生成すると表示されます)。
- **「model.blend がありません」**(オレンジ)と出るカードは、そのフォルダに `model.blend` が無いという意味です。Blender ファイルを入れて「↻ 再スキャン」すると解消します。
- 生成済みのカードは、**サムネイルの上でマウスを左右に動かす**とモデルがその位置に応じて回ります。→ [カードの上でモデルを回して見る](#カードの上でモデルを回して見る)
- サイドバー下部のボックスに**キャッシュの合計容量**が表示されます。

### カードの上でモデルを回して見る

カードのサムネイルにマウスを乗せ、**そのまま左右に動かす**と、モデルを一周ぐるりと回した画像に切り替わります。クリックしなくても、裏側やシルエットを確かめられます。

- **左端**がサムネイルと同じ角度で、右へ動かすほど一周します。動かしている間はカードの下端に細いバーが出て、今どのあたりを見ているかを示します。
- マウスを外すと元のサムネイルに戻ります。
- 生成がまだのアセットや `model.blend` が無いアセットでは何も起きません(静止したままです)。回したいアセットは [7. 生成する](#7-サムネイルとプレビューを生成する)で生成してください。
- タッチ操作の環境では働きません。詳細画面の [3D プレビュー](#3d-プレビュー)をお使いください。

### カード表示とリスト表示

右上の **[⊞ | ≡]** アイコンで切り替えられます。リスト表示ではポリゴン数・ファイルサイズ・更新日時が一列に並びます。

---

## 5. 探す・絞り込む

| 操作 | 方法 |
|------|------|
| **名前やタグで検索** | 上部の検索ボックスに入力するだけ。入力しながら絞り込まれます(アセット名とタグ名の両方にヒットします) |
| **カテゴリで絞り込み** | 左サイドバーでカテゴリ名をクリック。「すべてのアセット」で解除。上部の「カテゴリ▼」からも選べます |
| **タグで絞り込み** | サイドバーの「タグ」からタグ名をクリック。もう一度クリックするか、見出し横の「#タグ名 ×」で解除 |
| **並び替え** | 上部の並び順メニューから「カテゴリ・タイトル順」「更新日: 新しい順」「更新日: 古い順」を選択 |

検索・カテゴリ・タグ・並び順は**組み合わせて**使えます。

---

## 6. 新しいアセットを作る

1. 右上の **「＋ 新規アセット」** を押します。
2. 次の 3 つを入力します。
   - **タイトル**: アセットの名前(そのままフォルダ名になります)
   - **カテゴリ**: 並んでいるボタンから選ぶか、**「＋ 新規カテゴリ」** を押して新しい名前を入力
   - **テンプレート**: 元にする Blender ファイル。最初は `empty.blend`(空のシーン)だけがあります
3. 入力中は、これから作られるフォルダの中身がダイアログ下部にプレビュー表示されます。
4. **「✓ 作成する」** を押すと、一覧に新しいアセットが現れます。

作成されたアセットのフォルダには、model.blend のほかに `textures`(テクスチャ)、`references`(資料)、`renders`(レンダリング画像)フォルダと `notes.md`(メモ)が用意されています。

> **ヒント**: よく使うシーン設定(カメラやライト入り)の .blend ファイルをライブラリ内の `templates` フォルダに置くと、次回からテンプレートとして選べるようになります。

---

## 7. サムネイルとプレビューを生成する

作ったばかり・追加したばかりのアセットはサムネイルがありません。**生成**すると、サムネイル・3D プレビュー・ポリゴン数などの情報・一覧で回して見るための全周画像がまとめて作られます。

### 1 つだけ生成する

- サムネイルがまだ無いカードは、カード内の **「⚡ 生成」** ボタンを押します。
- すでにサムネイルがあるカードは、**カードにマウスを乗せる**と右下に **「再生成」**(要更新のときは「更新」)ボタンが現れます。
- 詳細画面のヘッダーにある **「↻ 再生成」** からでも生成できます。

### まとめて生成する

左サイドバー下部の **「⚡ 不足分を一括生成 (n)」** を押すと、**サムネイルが無いものと「要更新」のものだけ**が順番に処理されます(最新のものはスキップされるので、何度押しても無駄がありません)。括弧内の数字が、生成が必要なアセットの件数です。

### 生成中の表示

画面上部のツールバー右側に「**⟳ 生成中 2/5**」のような進行状況が表示されます。生成は 1 件ずつ順番に行われ、終わったものからサムネイルが表示されていきます。そのまま他の操作をしていて大丈夫です。

> **注意**: 生成には Blender が必要です。設定で「Blender実行ファイル」を入力していないと「生成に失敗しました」と表示されます。

---

## 8. 詳細画面でできること

一覧でカード(またはリストの行)をクリックすると詳細画面が開きます。詳細画面はサイドバーの無い全画面表示で、左上の **「←」** ボタンで一覧に戻ります。

画面は **左の大きな表示エリア** と **右の情報パネル(320px)** に分かれています。

### 3D プレビュー

生成済みのアセットは、その場で 3D モデルをぐるぐる回して見られます。操作方法は画面下部にも表示されています。

| 操作 | マウス |
|------|--------|
| 回転 | 左ボタンでドラッグ |
| 移動(パン) | Shift を押しながら左ドラッグ(右ドラッグでも可) |
| 拡大・縮小 | ホイールを回す |

右上には 5 つのツールボタンがあります。

| ボタン | はたらき |
|--------|----------|
| **⊞ グリッド** | 床のグリッド表示を on / off します |
| **↻ 自動回転** | on にするとモデルがゆっくり回り続けます。ドラッグしている間は止まり、離すとまた回り始めます |
| **🎚 表示設定** | 見た目を変えるパネルを開きます → [表示設定](#表示設定) |
| **📷 スクリーンショット** | 今見えている状態を「アセット名.png」として保存します。写るのはモデルだけで、画面上のボタン・バッジ・ギズモは写りません(背景を「透明」にしておくと、背景が抜けた PNG になります) |
| **⛶ 全画面** | プレビューを画面いっぱいに広げます(Esc で戻る) |

このほか、プレビューの隅に次の 2 つが出ています。

- **左上のバッジ**: 読み込んだ 3D データのファイルサイズと、表示の滑らかさ(FPS)
- **右下の XYZ ギズモ**: 軸(X / Y / Z)をクリックすると、カメラがその方向へ回り込みます。真横や真上から見たいときに便利です

まだ生成していない場合は「GLBが未生成です」と表示され、**「⚡ 生成」** ボタンが出ます。

### 表示設定

ツールボタンの **「🎚 表示設定」** を押すと、モデルの見え方を変えるパネルが開きます。

| 項目 | 説明 |
|------|------|
| **表示モード** | **マテリアル**(通常の見た目)/ **クレイ**(色や質感を外して形だけを見る。Blender のソリッド表示に近い)/ **ワイヤー**(線だけで表示) |
| **ワイヤー重ね** | 面の上にワイヤーフレームの線を重ねます。表示モードが「ワイヤー」のときは選べません(面が無いため) |
| **背景** | **ライト** / **ダーク** / **環境**(部屋の中に置いたような背景になり、照明も変わります)/ **透明**(市松模様で表示。スクリーンショットが透過 PNG になります) |
| **明るさ** | 画面全体の明るさ(露出)をスライダーで調整します |

> **注意**: ここで変えた設定は、グリッドと自動回転も含めて**保存されません**。一覧に戻ってから開き直すと、すべて元の状態に戻ります。

### 右の情報パネル

- **メタデータ**: Object 数、Polygon 数、Animation の有無など、Blender ファイルの中身の情報(生成すると表示されます)
- **タグ**: → [9. タグを付けて整理する](#9-タグを付けて整理する)
- **ファイル**: アセットフォルダの中身とファイルサイズの一覧。一番下に更新日時と作成日時が出ます

### ファイルビューア

右の情報パネルの **「ファイル」** で `textures/` `references/` `renders/` `notes.md` をクリックすると、左の表示エリアがそのファイルの表示に切り替わります(見るだけで、変更はされません)。`cache/glb/…` をクリックすると 3D プレビューに戻ります。

- **textures/**: テクスチャがグリッドで並び、種別(ALBEDO / NORMAL など)・解像度・サイズが出ます
- **references/**: 資料画像が段組みで並びます
- **renders/**: 選んだ 1 枚が大きく表示され、下のサムネイルで切り替えられます。「ダウンロード」で保存できます
- **notes.md**: 書いたメモが整形されて表示されます(見出し・箇条書き・チェックリスト・表など)

画像はクリックすると原寸で開きます。

### ヘッダーのボタン

- **「📁」(フォルダアイコン)**: このアセットのフォルダを Finder(Windows ではエクスプローラー)で開きます。テクスチャ画像を入れたいときなどに便利です
- **「↻ 生成 / 再生成」**: このアセットだけ生成します
- **「Blenderで開く」**: このアセットの model.blend を Blender で開きます

---

## 9. タグを付けて整理する

詳細画面の右パネルの **「タグ」** 欄で付け外しできます。

- **追加**: 末尾の **「＋」** を押すと入力欄が出ます。タグ名を入れて Enter(Esc で取り消し)
- **削除**: タグにマウスを乗せると出る **×** をクリック

付けたタグはアセットフォルダ内の `meta.json` に保存されるので、**アプリを入れ直してもタグは消えません**。サイドバーのタグ一覧にも自動で反映されます。

---

## 10. ファイルを直接追加・削除したとき(再スキャン)

アプリを通さずに、Finder / エクスプローラーで直接ファイルを操作しても大丈夫です。

- **アセットを追加したい**: ライブラリの `source` フォルダ内のカテゴリフォルダの中に、アセットのフォルダを作って `model.blend` を入れる
- **アセットを削除・名前変更・カテゴリ移動したい**: フォルダごと削除・リネーム・移動する

そのあと左サイドバー下部の **「↻ 再スキャン」** を押すと一覧に反映されます(アプリの起動時にも自動で反映されます)。

---

## 11. 「要更新」と表示されたら

カードのサムネイル右上にオレンジの **「↻ 要更新」** バッジが付くことがあります。これは「Blender でファイルを保存し直したので、サムネイルやプレビューが古くなっていますよ」という意味です。

アプリを新しくした直後は、まだ**全周画像を持っていないアセット**にもこのバッジが付きます(一覧でモデルを回して見る機能のためのデータです)。同じように生成し直せば消えます。

- そのカードにマウスを乗せて **「更新」** を押すか、
- 左サイドバー下部の **「⚡ 不足分を一括生成」** を押す(要更新のものだけまとめて作り直されます)

と最新の状態に戻ります。設定で**サムネイルサイズを変更した場合**も、すべてのアセットが「要更新」になります。同じように一括生成すれば新しいサイズで作り直されます。

---

## 12. キャッシュの容量確認と削除

サムネイルやプレビューのデータ(キャッシュ)の合計容量は、左サイドバー下部のボックスに常に表示されています。

容量が気になったら削除できます:

1. サイドバー最下段の **「設定」** を開く
2. 「キャッシュ・生成」の **「🗑 キャッシュを削除」** を押す
3. 確認メッセージで OK を押す

削除しても **作品データとタグは一切消えません**。一覧はサムネイル無しの状態に戻るだけで、また「⚡ 不足分を一括生成」を押せば全部作り直せます。

---

## 13. 困ったときは

| 症状 | 対処 |
|------|------|
| ブラウザが自動で開かない | ブラウザで `http://127.0.0.1:8765` を開く |
| 「ライブラリディレクトリが設定されていません」 | [3. はじめての設定](#3-はじめての設定) を行う |
| 「生成に失敗しました」と出る | 設定の「Blender実行ファイル」の場所が正しいか確認する。Blender 5.2 LTS 以上が必要 |
| macOS で「開発元を検証できないため開けません」と出て起動できない | 「システム設定 → プライバシーとセキュリティ」を開き、下の方に出る **「このまま開く」** を押す(初回のみ)。ダウンロードや AirDrop で受け取ったアプリに macOS が付ける印が原因で、アプリの中身の問題ではありません |
| macOS で「Blender が見つかりません」と保存できない | 「Blender実行ファイル」に `/Applications/Blender.app/Contents/MacOS/Blender` を入力する。`Blender.app` はフォルダで、本体はその中にあります(`/Applications/Blender.app` と入力した場合は保存時に自動で読み替えます) |
| 追加したはずのアセットが一覧に出ない | サイドバーの「↻ 再スキャン」を押す。それでも出ない場合は、`source` の**カテゴリフォルダの直下**にアセットのフォルダがあるか確認する(深い階層は認識されません) |
| カードに「model.blend がありません」と出る | そのアセットのフォルダに `model.blend` という名前の Blender ファイルを入れて再スキャンする |
| サムネイルが「要更新」のまま | カードの「更新」またはサイドバーの「⚡ 不足分を一括生成」を押す |
| サムネイルが色の付かない灰色になる | テクスチャ画像が見つからないと、色を諦めて灰色のサムネイルを作ります。使っている画像をそのアセットの **`textures` フォルダ**に入れて生成し直すか、Blender で「ファイル > 外部データ > リソースをパック」してから保存し直してください |
| 動きがおかしい・表示が崩れた | アプリを終了(黒い画面を閉じる)して、もう一度起動する。データは `source` フォルダに安全に保存されています |

### バックアップについて

ライブラリの **`source` フォルダだけ**をコピーしておけば、作品・タグを含めてすべて復元できます。`cache` フォルダと `database.db` はコピー不要です(自動で作り直されます)。
